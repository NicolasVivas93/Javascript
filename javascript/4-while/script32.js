/* Realizar un programa que imprima en pantalla los números del 1 al 100. */

let contador = 1;

while (contador <= 100) {
    document.write(contador);
    document.write('<br>');
    contador = contador + 1;    
}